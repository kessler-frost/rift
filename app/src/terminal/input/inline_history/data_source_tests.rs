// All tests in this module exercised the removed conversation-interleaving
// feature (`interleave_conversations`, `MenuItem::Conversation`, and the
// `order`/`sort_timestamp` fields on `MenuEntry`) and were deleted along with it.
